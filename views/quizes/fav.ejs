<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">

</head>
<body>

	<table style="margin: 0 auto;">

				<%for(i in quizes){
				for(index in quizes[i].Favoritos){%>
				
				
		
			<tr>
			<td><a href="/quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td>
			<%if(session.user){
          if (session.user.id ) {%>
			<td><a href="/quizes/<%= quizes[i].id %>/edit"><button>editar</button></a></td>
			<td>
			<form method="post" action="/quizes/<%= quizes[i].id %>?_method=delete">
			    <button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">
			    Borrar
			    </button>
			</form>
			</td>
			<td><form method="post" action="/user/<%= session.user.id %>/favoritos/<%= quizes[i].id %>?_method=put" enctype="multipart/form-data"><button>fav</button></form>
<td><form method="post" action="/user/<%= session.user.id %>/favoritos/<%= quizes[i].id %>?_method=delete" enctype="multipart/form-data"><button>Unfav</button></form></td>
</td>
			<%}}%>
			</tr>
		<% } %>
	</table><p>
<%if(session.user){%>
<a href="/quizes/new"><button type="button">Crear Pregunta</button></a>
<%}%>
</body>
</html>
